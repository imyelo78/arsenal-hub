name = "arsenal-hub"
compatibility_date = "2024-01-01"

# D1 database (for production, replace database_id with your Cloudflare D1 ID)
[[d1_databases]]
binding = "DB"
database_name = "arsenal-hub-db"
database_id = "local"
migrations_dir = "migrations"
